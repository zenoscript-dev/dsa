// first we need to create a node;
class Node{
    // we need to accept data
    constructor(data){
        this.head = data; //we will store the data inside head.
        this.next = null; // by default next will be null inside a node.
    }
}

// linked list
class LinkedList{
    constructor(){
        // first elemet of the linked list is called head of the linked list;
        this.head = null;
    }
}
